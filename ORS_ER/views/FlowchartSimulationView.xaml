<UserControl x:Class="ORS_ER.views.FlowchartSimulationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:skia="clr-namespace:SkiaSharp.Views.WPF;assembly=SkiaSharp.Views.WPF"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <ListView x:Name="LayersListView"
              ItemsSource="{Binding Items}"
              Margin="5"
              ScrollViewer.VerticalScrollBarVisibility="Hidden"
              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
              ScrollViewer.CanContentScroll="True">

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,5" Width="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=ActualWidth}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" 
                               Text="{Binding Name}" 
                               FontWeight="Bold"
                               TextWrapping="Wrap"
                               Margin="0,0,0,2"/>

                            <TextBlock Grid.Row="1" 
                               Text="{Binding Description}" 
                               TextWrapping="Wrap"
                               Margin="0,0,0,2"/>

                            <TextBlock Grid.Row="2" 
                               Text="{Binding Category}" 
                               Foreground="Gray"
                               TextWrapping="Wrap"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

        <skia:SKElement x:Name="skiaElement"
                        Grid.Column="1"
                        PaintSurface="OnPaintSurface"
                        MouseDown="SkiaElement_OnMouseDown"
                        MouseMove="SkiaElement_OnMouseMove"
                        MouseUp="SkiaElement_OnMouseUp"
                        MouseWheel="SkiaElement_OnMouseWheel"/>

        <TextBox x:Name="ConsoleOutput" Grid.Column="2"
                 Text="------Console Output------&#x0a;"
                 Margin="5"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"/>
    </Grid>
</UserControl>
