<UserControl x:Class="ORS_ER.views.FlowchartSimulationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:skia="clr-namespace:SkiaSharp.Views.WPF;assembly=SkiaSharp.Views.WPF"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Margin="8" CornerRadius="8" BorderBrush="#2A000000" BorderThickness="1" Background="#FFF7F7F7" Padding="8">
            <DockPanel LastChildFill="True">
                <TextBlock DockPanel.Dock="Top" Text="Components" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8"/>

                <ListView x:Name="LayersListView"
                         ItemsSource="{Binding Items}"
                         Margin="0"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.CanContentScroll="True">

                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                    </ListView.ItemContainerStyle>      

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,8" Padding="8" Background="White" BorderBrush="#22000000" BorderThickness="1" CornerRadius="6" HorizontalAlignment="Stretch">
                                <StackPanel HorizontalAlignment="Stretch">
                                    <Border Width="56" Height="56" CornerRadius="8" Background="#FFF2F2F2" BorderBrush="#22000000" BorderThickness="1" HorizontalAlignment="Center" Margin="0,0,0,8">
                                        <skia:SKElement Tag="{Binding}" Loaded="PalettePreviewElement_Loaded" />
                                    </Border>

                                    <StackPanel HorizontalAlignment="Stretch">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" TextAlignment="Center" />
                                        <TextBlock Text="{Binding Description}" Foreground="#99000000" TextWrapping="Wrap" Margin="0,2,0,0" TextAlignment="Center" />
                                        <TextBlock Text="{Binding Category}" Foreground="#66000000" Margin="0,4,0,0" TextAlignment="Center" />
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </DockPanel>
        </Border>

        <Border Grid.Column="1" Margin="8" CornerRadius="8" BorderBrush="#2A000000" BorderThickness="1" Background="White" SnapsToDevicePixels="True">
            <skia:SKElement x:Name="skiaElement"
                            PaintSurface="OnPaintSurface"
                            MouseDown="SkiaElement_OnMouseDown"
                            MouseMove="SkiaElement_OnMouseMove"
                            MouseUp="SkiaElement_OnMouseUp"
                            MouseWheel="SkiaElement_OnMouseWheel"/>
        </Border>

        <Border Grid.Column="2" Margin="8" CornerRadius="8" BorderBrush="#2A000000" BorderThickness="1" Background="White" Padding="6">
            <TextBox x:Name="ConsoleOutput"
                     Text="------Console Output------&#x0a;"
                     Margin="0"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     IsReadOnly="True"
                     VerticalScrollBarVisibility="Auto"/>
        </Border>
    </Grid>
</UserControl>
